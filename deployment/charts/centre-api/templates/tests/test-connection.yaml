apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "centre-api.fullname" . }}-test-connection"
  labels:
    {{- include "centre-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "centre-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
