apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "centre-web.fullname" . }}-test-connection"
  labels:
    {{- include "centre-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "centre-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
